import 'package:flutter/material.dart';

class RetweetWidget extends StatelessWidget {
  const RetweetWidget({super.key, this.index});
  final dynamic index;
  @override
  Widget build(BuildContext context) {
    return Column(children: [

    ],);
  }
}
